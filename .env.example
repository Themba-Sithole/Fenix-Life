# =============================================================================
# Fenix Life — Environment Variables
# Copy this file to `.env` in the project root and fill in your values.
# Never commit `.env` to git.
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Neon — free tier)
# Sign up: https://neon.tech
# Dashboard → Project → Connection string → copy "Pooled connection"
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"
# Use Neon's **Pooled connection** string (host contains `-pooler`) for local API + Render.

# -----------------------------------------------------------------------------
# API SERVER
# -----------------------------------------------------------------------------
PORT=3001
NODE_ENV=development

# Generate a random secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="replace-with-a-long-random-string"

# Comma-separated URLs allowed to call the API (add your Vercel URL after deploy)
CORS_ORIGINS="http://localhost:5173,http://localhost:5174,http://localhost:4173"

# -----------------------------------------------------------------------------
# REDIS (Upstash — free tier)
# Dashboard → Fenix Life → REST tab
# -----------------------------------------------------------------------------
UPSTASH_REDIS_REST_URL="https://YOUR-DB.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-upstash-rest-token"
# Optional: standard Redis URL for BullMQ (later)
# REDIS_URL="rediss://default:TOKEN@YOUR-DB.upstash.io:6379"

# -----------------------------------------------------------------------------
# FRONTEND (Vite — used at build time on Vercel)
# Local: http://localhost:3001/v1
# Production: https://YOUR-API.onrender.com/v1
# -----------------------------------------------------------------------------
VITE_API_URL="http://localhost:3001/v1"

# -----------------------------------------------------------------------------
# ADMIN SEED (one-time local setup — Doc 42 §5.4)
# Run: npm run admin:seed
# -----------------------------------------------------------------------------
ADMIN_SEED_EMAIL="admin@fenix.local"
ADMIN_SEED_PASSWORD="change-me-to-a-secure-password"
